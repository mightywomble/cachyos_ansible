---
all:
  children:
    cachyos_systems:
      hosts:
        localhost:
          ansible_connection: local
          # Override gpu_type for this host if needed:
          # gpu_type: amd  # or 'nvidia' or 'none'
